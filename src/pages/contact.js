import * as React from 'react';

const ContactPage = (props) => {
  return <p>Hi there! This is the about me page</p>;
};

export const Head = () => <title>Contact Page</title>;

export default ContactPage;
